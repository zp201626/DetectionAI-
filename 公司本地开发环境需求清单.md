# 公司本地开发环境需求清单

本文档用于将 DetectionAI 助手部署到公司内网开发环境。在正式接入公司数据前，应先完成网络、模型网关、知识文件和数据存储的确认。

## 一、当前代码已经具备的内容

| 项目 | 当前状态 | 说明 |
| --- | --- | --- |
| FastAPI 后端 | 已具备 | 提供健康检查、聊天、文件上传和图片解析接口 |
| React + Vite 前端 | 已具备 | 提供对话页面、快捷问题和来源区域 |
| LangGraph 工作流 | 已具备基础版本 | 已有问题路由、证据收集和答案生成流程 |
| 本地 Detection 演示数据 | 已具备 | 用于 lot、缺陷、SPC、报警和 recipe 变更演示 |
| 本地知识检索 | 已具备基础版本 | 当前为轻量文本检索，后续需要替换为生产级向量检索 |
| 公司网关适配器 | 已预留 | 代码位置为 `backend/gateway.py` |
| 视觉模型接口 | 已预留 | 代码位置为 `backend/gateway.py` 和 `backend/service.py` |
| 来源引用结构 | 已具备基础版本 | 当前支持知识文档、lot、缺陷和 SPC 证据引用 |

## 二、公司必须提供的内容

### 1. AI 网关信息

这是接入公司模型最重要的前置条件。

请提供以下信息：

| 配置项 | 必须提供 | 示例 |
| --- | --- | --- |
| 网关基础地址 | 是 | `https://ai-gateway.company/v1` |
| 鉴权方式 | 是 | Bearer Token、API Key、内部签名或其他方式 |
| 鉴权请求头名称 | 是 | `Authorization` 或公司自定义请求头 |
| 文本模型接口路径 | 是 | `/chat/completions` 或公司自定义路径 |
| 视觉模型接口路径 | 如果支持图片解析则必须 | `/chat/completions` 或视觉专用路径 |
| Embedding 接口路径 | 使用向量检索时必须 | `/embeddings` 或公司自定义路径 |
| 请求格式 | 是 | OpenAI 兼容格式或公司自定义 JSON |
| 返回格式 | 是 | 需要说明回答文本、向量和错误字段位置 |
| TLS / 证书要求 | 内网环境建议提供 | 公司 CA 证书或双向 TLS 要求 |
| 超时和限流规则 | 是 | 单次请求超时、每分钟请求数、最大文件大小 |
| 测试环境地址 | 强烈建议 | 与生产网关隔离的开发地址 |

当前代码默认按 OpenAI 兼容接口设计。如果公司网关协议不同，只修改 `backend/gateway.py`，不修改 Detection 业务流程。

### 2. 需要确认的模型

至少需要确认以下三类模型：

#### 文本大模型

用于问题理解、知识整合、GraphRAG 结果解释和最终答案生成。

需要确认：

- 模型名称和版本
- 上下文长度
- 是否支持中文和中英文混合半导体术语
- 是否支持结构化 JSON 输出
- 是否支持流式输出
- 单次输入和输出 Token 限制
- 是否允许发送内部知识库内容

#### Embedding 向量模型

用于 PDF、Word、PPT、Excel、TXT、Markdown 等知识片段的向量检索。

需要确认：

- 模型名称和版本
- 向量维度
- 最大输入长度
- 是否支持批量向量化
- 是否支持中英文、缩写、机台编号和工艺术语
- 是否允许离线部署

#### 视觉模型

用于膜层结构图、缺陷示意图和文档内嵌图片解析。

需要确认：

- 模型名称和版本
- 支持的图片格式和最大图片大小
- 是否支持 Base64 或图片 URL
- 是否能识别中文标注、膜层名称和厚度数字
- 是否能返回结构化 JSON
- 是否保存图片或只做即时分析
- 图片是否允许离开公司内网

## 三、知识库数据需求

### 1. 四类必需文件

| 类别 | 内容示例 | 建议格式 |
| --- | --- | --- |
| 历史 Case | 缺陷处理记录、异常复盘、8D、良率异常分析 | PDF、Word、Excel、PPT、Markdown |
| 机台文件 | SOP、上下机规范、操作注意事项、标准排查流程 | PDF、Word、TXT、Markdown |
| 光学 Paper | 光学理论、膜层结构、缺陷光学机理、技术论文 | PDF、Word、PPT、图片 |
| 其他资料 | 临时作业文件、工程规范、培训资料、术语表 | PDF、Word、Excel、TXT、Markdown |

### 2. 每个文件最好提供的元数据

- 文件名称
- 文件版本
- 生效日期
- 失效日期
- 文档分类
- 所属工艺段
- 所属机台或机台类型
- 所属产品和层别
- 文档负责人
- 是否允许 AI 检索
- 是否包含敏感信息

### 3. 引用溯源要求

为了满足回答可追溯，解析文档时需要保留：

- 原始文件名
- 页码
- 章节标题
- 段落编号或文本片段
- 表格编号
- 图片编号
- 图片所在页码
- 文档版本

如果原始文件是扫描 PDF，还需要 OCR 处理，否则无法稳定返回页码和原文引用。

## 四、向量数据库需求

当前代码使用本地轻量检索作为演示，正式环境需要确定向量数据库。

可选方案：

| 方案 | 适用场景 | 需要公司提供 |
| --- | --- | --- |
| Chroma | 单机开发和小规模验证 | 本地磁盘目录和备份策略 |
| Milvus | 内网多人使用和较大知识库 | 服务地址、账号、集合权限 |
| Elasticsearch / OpenSearch | 公司已有检索平台 | 集群地址、索引权限和向量插件 |
| PGVector | 公司已有 PostgreSQL | 数据库地址、Schema 和账号 |

需要确认：

- 文档数量和总容量
- 预计知识片段数量
- 是否需要增量更新
- 是否需要删除旧版本文档
- 是否需要按部门、产品、工艺和权限过滤
- 是否需要备份和恢复

## 五、GraphRAG 图谱需求

当前代码已预留 GraphRAG 方向，但生产环境还需要建设实体和关系存储。

### 1. 推荐实体

- 缺陷类型
- 缺陷模式
- 光学原理
- 膜层
- 膜层厚度
- 产品
- 层别
- 工艺步骤
- 检测机台
- Recipe
- SOP
- 历史 Case
- 排查步骤
- Paper

### 2. 推荐关系

- 缺陷类型 `对应` 光学原理
- 光学原理 `影响` 膜层结构
- 缺陷类型 `发生于` 工艺步骤
- 缺陷类型 `出现在` 检测机台
- 历史 Case `类似于` 当前问题
- SOP `建议排查` 缺陷类型
- Paper `支持解释` 光学原理
- Recipe `应用于` 检测机台

### 3. 需要公司确认的图数据库

- 开发版是否使用 NetworkX 或 SQLite
- 测试版是否使用 Neo4j
- 公司是否已有图数据库平台
- 是否允许建立独立的 Detection 知识图谱
- 图谱数据是否需要人工审核
- 图谱更新频率和责任人

## 六、图片和多模态处理需求

公司需要确认：

- 允许上传的文件格式：PNG、JPG、JPEG、PDF 内嵌图片等
- 单张图片最大大小
- 单次最多上传图片数量
- 是否需要自动压缩和脱敏
- 是否需要保留原图
- 是否需要保存解析后的结构化结果
- 是否需要图片原文来源、文件名和页码
- 图片中是否可能包含产品型号、内部机台编号或其他敏感信息
- 是否允许视觉模型将图片发送到公司 AI 网关以外的服务

## 七、公司内网部署需求

### 1. 基础运行环境

- Windows 或 Linux 服务器
- Python 3.10 或更高版本
- Node.js LTS
- Git
- 内网包管理或 PyPI / npm 镜像
- 至少一个可写的数据目录
- 可访问公司 AI 网关的网络权限

### 2. 推荐开发端口

| 服务 | 默认端口 | 用途 |
| --- | --- | --- |
| FastAPI | 8000 | 后端 API |
| React + Vite | 5173 | 前端开发页面 |
| Chroma 或其他向量库 | 按部署方案决定 | 向量检索 |
| Neo4j | 按部署方案决定 | GraphRAG 图谱 |

### 3. 网络和安全

- 前端到 FastAPI 的访问权限
- FastAPI 到公司 AI 网关的访问权限
- FastAPI 到向量数据库的访问权限
- FastAPI 到图数据库的访问权限
- 内网 DNS 或固定 IP
- HTTPS 证书
- 防火墙放行策略
- 代理服务器配置
- 公司 CA 证书

## 八、权限和审计需求

正式接入公司资料前，需要确认：

- 是否需要登录和单点登录
- 用户角色：Detection 工程师、Process 工程师、Yield 工程师、管理员
- 不同角色可以访问哪些知识文件
- 是否记录用户问题和 AI 回答
- 是否记录文件上传和图片解析行为
- 日志保留周期
- 敏感信息脱敏规则
- 是否禁止把答案自动写回 MES 或其他生产系统
- 是否需要人工确认后才能生成报告

## 九、目前代码还缺少的生产能力

下面这些功能需要在公司环境确认后继续开发：

1. 真实 PDF、Word、PPT、Excel 解析和页码级引用。
2. 生产级 Embedding 和向量数据库接入。
3. GraphRAG 实体抽取、关系构建和图数据库接入。
4. 视觉模型返回膜层、厚度和结构关系的结构化 JSON。
5. 文档上传后的异步解析、切分、向量化和入库任务。
6. 文档版本管理、重复文件识别和旧版本下线。
7. 公司登录、角色权限和审计日志。
8. 长回答流式输出、任务状态和失败重试。
9. 生产数据脱敏、访问控制和数据留存策略。
10. 压力测试、答案评测和 Detection 工程师验收集。

## 十、建议公司先提供的最小信息包

为了开始公司本地联调，建议先提供以下内容：

1. AI 网关测试地址。
2. AI 网关鉴权方式和测试 Token。
3. 文本模型名称。
4. Embedding 模型名称。
5. 视觉模型名称。
6. 一份脱敏历史 Case。
7. 一份脱敏机台 SOP。
8. 一份光学 Paper 或技术资料。
9. 一张脱敏膜层结构图。
10. 公司开发机的 Python、Node.js 和网络限制说明。

## 十一、联调验收标准

至少准备以下测试问题：

- 根据一个历史 Case 找到相似案例，并给出来源。
- 根据 defect 类型解释对应的光学原理，并引用 Paper。
- 根据膜层结构图识别膜层顺序和厚度，并说明不确定项。
- 查询某机台 SOP 的标准排查流程，并给出页码或章节。
- 将理论、历史 Case、SOP 和图片解析结果合并为一份 Detection 分析报告。

每个测试都应检查：

- 回答是否贴合 Detection 场景。
- 是否引用了真实来源。
- 页码或章节是否准确。
- 是否区分事实、推测和不确定信息。
- 是否避免编造不存在的 Case、参数和文献。
- 是否没有执行机台参数修改或生产控制动作。
