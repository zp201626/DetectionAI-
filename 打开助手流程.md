# 打开助手流程

本文档用于启动 DetectionAI 助手。项目由 FastAPI 后端和 React + Vite 前端组成，需要分别启动两个服务。

## 一、第一次使用前的准备

### 1. 安装 Python

建议使用 Python 3.10 或更高版本。安装时勾选 **Add Python to PATH**。

### 2. 安装 Node.js

前端依赖 `npm`，`npm` 会随 Node.js 一起安装。

请从 Node.js 官网下载安装 LTS 版本：

https://nodejs.org/

安装完成后，必须关闭原来的 PowerShell，再重新打开一个 PowerShell，检查安装是否成功：

```powershell
node -v
npm -v
```

两个命令都显示版本号后，才可以启动前端。

## 二、启动后端

打开第一个 PowerShell 窗口，逐行执行：

```powershell
cd "C:\Users\Maya\Documents\Codex\DetectionAI助手\AI助手"
```

如果项目中还没有虚拟环境，第一次执行：

```powershell
python -m venv .venv
```

安装后端依赖：

```powershell
.\.venv\Scripts\python.exe -m pip install -r backend\requirements.txt
```

启动后端：

```powershell
.\.venv\Scripts\python.exe -m uvicorn backend.main:app --reload --port 8000
```

看到类似以下信息，表示后端启动成功：

```text
Uvicorn running on http://127.0.0.1:8000
```

不要关闭这个窗口。

打开下面地址检查后端：

http://127.0.0.1:8000/api/health

正常情况下会显示包含以下内容的 JSON：

```json
{
  "status": "ok",
  "local_fallback_enabled": true
}
```

## 三、启动前端

打开第二个 PowerShell 窗口，逐行执行：

```powershell
cd "C:\Users\Maya\Documents\Codex\DetectionAI助手\AI助手\frontend"
```

第一次启动时安装前端依赖：

```powershell
npm install
```

安装完成后启动前端：

```powershell
npm run dev
```

看到类似以下信息，表示前端启动成功：

```text
Local: http://localhost:5173/
```

浏览器打开：

http://localhost:5173

不要直接双击 `frontend/index.html`，因为 React + TypeScript 文件需要由 Vite 编译后才能运行。

## 四、日常再次打开助手

以后不需要重复安装依赖，只需要启动两个服务。

第一个窗口：

```powershell
cd "C:\Users\Maya\Documents\Codex\DetectionAI助手\AI助手"
.\.venv\Scripts\python.exe -m uvicorn backend.main:app --reload --port 8000
```

第二个窗口：

```powershell
cd "C:\Users\Maya\Documents\Codex\DetectionAI助手\AI助手\frontend"
npm run dev
```

然后访问：

http://localhost:5173

## 五、常见问题

### PowerShell 报错 Activate.ps1 被禁止

不要执行虚拟环境激活脚本，直接使用下面的方式启动后端：

```powershell
.\.venv\Scripts\python.exe -m uvicorn backend.main:app --reload --port 8000
```

### npm 无法识别

先关闭 PowerShell，再重新打开。然后检查：

```powershell
node -v
npm -v
```

如果仍然无法识别，重新安装 Node.js LTS，并确认安装程序启用了加入 PATH 的选项。

### 5173 端口被占用

Vite 会自动切换到其他端口，例如 `5174`。使用终端显示的 `Local` 地址访问即可。

### 浏览器显示空白页面

确认以下事项：

1. 后端窗口仍在运行。
2. 前端窗口仍在运行。
3. 浏览器访问的是 `http://localhost:5173` 或终端显示的 Vite 地址。
4. 没有直接打开 `frontend/index.html` 文件。

## 六、公司网关配置

当前未配置公司网关时，助手使用本地 Detection 工作流和演示知识库运行。

接入公司网关时，参考 `backend/.env.example` 配置：

```text
DETECTION_GATEWAY_BASE_URL=公司网关地址
DETECTION_GATEWAY_API_KEY=公司网关密钥
DETECTION_CHAT_MODEL=文本模型名称
DETECTION_VISION_MODEL=视觉模型名称
DETECTION_EMBEDDING_MODEL=向量模型名称
```

公司网关适配代码集中在：

```text
backend/gateway.py
```

业务调用集中在：

```text
backend/service.py
```
