# DetectionAI 助手启动说明

本版本采用 FastAPI 后端和 React + Vite 独立前端。  # 说明当前架构。

## 后端启动

```powershell
cd "C:\Users\Maya\Documents\Codex\DetectionAI助手\AI助手"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r backend\requirements.txt
uvicorn backend.main:app --reload --port 8000
```

## 前端启动

```powershell
cd "C:\Users\Maya\Documents\Codex\DetectionAI助手\AI助手\frontend"
npm install
npm run dev
```

浏览器打开 `http://localhost:5173`。  # 说明前端访问地址。

## 公司网关配置

复制 `backend/.env.example` 中的变量到当前终端环境，或者使用项目根目录的 `.env` 管理工具注入。  # 说明网关配置方式。

当前模型分工建议为：`glm-5.2` 负责复杂主回答，`glm-5.3-flash` 负责快速任务，`kimi-k2.6-cloud` 负责长上下文，`doubao-seed-2.0-pro-cloud` 负责图片解析，`qwen3-Embedding` 负责向量库。  # 说明模型分工。

API Key 只能写入项目根目录的 `.env`，不要写入代码、截图或 GitHub。  # 强调敏感凭证保护要求。

网关适配代码集中在 `backend/gateway.py`，业务调用集中在 `backend/service.py`。  # 指出未来对接位置。

如果不配置网关，当前版本仍可以使用本地规则工作流和本地知识库进行演示。  # 说明离线兜底行为。
